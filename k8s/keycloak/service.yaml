apiVersion: v1
kind: Service
metadata:
  name: keycloak
spec:
  type: NodePort
  selector: { app: keycloak }
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30081   # mapeado para host:8081 via extraPortMappings do Kind