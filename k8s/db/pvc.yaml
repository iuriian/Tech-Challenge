# k8s/db/pvc.yaml  (equivalente ao volume nomeado "postgres_data")
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi