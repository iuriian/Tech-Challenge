apiVersion: v1
kind: Service
metadata:
  name: db          # o app/keycloak/sonarqube já usam "db:5432" — nome do Service = nome do Compose
spec:
  selector: { app: db }
  ports:
    - port: 5432
      targetPort: 5432