#apiVersion: v1
#kind: Service
#metadata: { name: app }
#spec:
#  selector: { app: app }
#  ports:
#    - { port: 8080, targetPort: 8080 }

apiVersion: v1
kind: Service
metadata: { name: app }
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30083